package com.example.mygrub.user;

import java.util.ArrayList;
import java.util.HashMap;
import java.util.Map;

public class RWDetails {
    public String email, password, privilege, name, gender, firstName, lastName, OKU, address1,
            address2, birthdate, employmentStatus, imageUrl, income, maritalStatus, nationality,
            organization, postcode, state, suspended, town, verify, complete_datetime, start_datetime,
            race, religion;

    public String title, quantity, unit, location, contact, desc, status;
    public String endDate, startDate, qrEnabled, qrUrl, type, username, userID;
    public Map<String, String> tags;

    public RWDetails() {
    }

}
